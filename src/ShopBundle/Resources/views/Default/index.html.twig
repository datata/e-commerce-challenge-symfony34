{% extends 'base.html.twig' %}
{% block body %}

    <div class="container">
        <div class="row">
            {% for product in products%}

                <div class="col" style="color:black; padding:3em;">
                    <a href="#">
                        <div class="card mx-auto" style="width: 15rem;">
                            <img alt="..." class="card-img-top" src="../img/{{product.category}}">
                            <div class="card-body">
                                <h5 class="card-title text-center">{{product.product}}</h5>
                                <p class="card-text text-center">{{product.price}}€</p>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}
