{% extends 'base.html.twig' %}

{% block body %}
    <h1>Brand edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-warning" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    
            <a class="btn btn-primary" href="{{ path('brand_index') }}">Back to the list</a>
        
            <a class="btn btn-warning" href="{{ path('brand_delete', { 'id': brand.id }) }}">Delete</a>

{% endblock %}
